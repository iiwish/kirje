# T202A-A001 Attempt

## Outcome

- Status: GREEN implementation complete; review remediation required.
- Scope: only packet-allowed Cargo, core, store, schema, and test files changed.
- Acceptance: not accepted. Independent review found four High and one Medium
  gap after all initial gates passed.

## TDD Evidence

The initial targeted test first failed for the missing `OwnerPublicKey`,
authority APIs, and canonical DDL. After implementation, 14 core authorization
tests and 11 authority tests passed together with every packet gate.

## Blocking Review Findings

1. `confirm_anchor` did not require the exact `ConfirmationRequired` state and
   opened anchor context.
2. The active trust-epoch bundle was not compared with both meta and the
   recomputed canonical bundle.
3. Authority events checked only self-hashes rather than exact canonical rows,
   ordering, and state transitions.
4. Confirmation did not reclassify identity/version inside its write
   transaction after a stale open.
5. The raw SQL invariant matrix did not yet execute each table's required
   storage, length, range, enum, relation, and partial-unique boundaries.

These findings became T202A-A002 RED tests; no weakened assertion was accepted.
