# T202A-A002 Attempt

## Outcome

- Status: GREEN implementation complete; review remediation required.
- Scope: only `authority.rs` and `authority_schema.rs` changed in this attempt.
- Acceptance: not accepted. The original five findings closed, but independent
  review found one High and one Medium stale/concurrency path.

## TDD Evidence

Five deterministic RED failures covered active bundle corruption, pending event
sequence corruption, extra historical keys, confirmation from the wrong state,
and stale-open application ID replacement. A further RED test covered tampered
`sqlite_sequence`. The final authority suite passed 20/20 and all packet gates.

## Closed Findings

- Exact confirmation state/context and one-shot behavior.
- Meta/epoch/recomputed trust-bundle equality.
- Exact bootstrap/confirmation events, canonical transcript, row count, order,
  and sequence high-water.
- Identity/version reclassification inside confirmation's write transaction.
- Executable 17-table NULL/storage/length/range/enum/relation/partial-unique and
  composite-FK matrix.
- Actual second entropy-read failure rollback and clean restart.

## New Blocking Findings

1. A stale non-Unconfigured store could generate replacement identities after
   the committed database disappeared or became pristine.
2. Open validation used several autocommit reads and could combine pending meta
   with ready events during a concurrent confirmation.

These findings became T202A-A003 RED tests.
