# T202A-A004 Attempt

## Outcome

- Status: Accepted after independent security, engineering, and QA review.
- Scope: only `authority.rs` and `authority_schema.rs` changed in this attempt.
- Result: no Critical, High, or Medium finding remains. One Low canonical-DDL
  whitespace lint item is recorded without changing the frozen bytes.

## TDD Evidence

Three independent RED tests exited 101 before the fix:

- a valid authority database switched to DELETE mode remained in DELETE after
  open;
- stale confirmation and exact-retry write paths did not restore WAL;
- stale Unconfigured preflight changed a foreign database from DELETE to WAL
  before rejecting its application ID.

The accepted connection flow opens existing files without CREATE, performs a
non-mutating identity/version/pristine preflight, enables and verifies WAL/FULL
only for KIRJ v1 or legitimate first initialization, and then starts a new
deferred or immediate transaction that repeats complete classification and
validation. Foreign, newer, nonempty zero-ID, and stale pristine databases keep
their exact fingerprint.

## Final Validation

- Authority contract: 26/26.
- Core authorization contract: 14/14.
- All seven packet gates: pass.
- Workspace tests, Clippy with warnings denied, and build: pass.
- Canonical DDL digest and 17/14/3 inventory: exact.
- License, ban, source/governance diff, scope, unsafe, and privacy scans: pass.
- Independent final review: no blocking finding.

The pre-existing yanked protocol dependency is recorded in the aggregate test
results and remains outside T202A's dependency path and change scope.
