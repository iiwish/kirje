# T202B-A004 Attempt

## Outcome

- Status: Accepted after independent security, engineering, and QA review.
- Scope: only `authority.rs` and `authority_authorization.rs` changed in this
  remediation attempt.
- Final review: Critical 0, High 0, Medium 0, Low 0.

## TDD Evidence

- Orphan-event stale-handle test exited 101 because create returned
  `AuthorizationMalformed` instead of `OwnerRecoveryRequired`.
- Append-before-link fault test exited 101 because
  `ChallengeCreatedEventAppended` was absent.
- Validator-boundary mapping now preserves ordinary caller lookup and
  StoreRead/I/O semantics while classifying durable cross-row corruption as
  recovery-required.
- The new test-only fault point runs after event insertion/sequence acquisition
  and before link update; every challenge precommit point consumes exactly 48
  bytes and rolls challenge, link, event, and clock state back atomically.

## Final Validation

- Core authorization: 16/16.
- Authority schema: 27/27.
- Authority authorization: 33/33.
- Core/store full: 142/142; no-default: 19/19.
- Packet, workspace test/Clippy/build, Rust 1.88, canonical DDL, artifact,
  scope, privacy, and whitespace gates pass.
- No blocking or Low finding remains.
