# T202C3-A005 Credential Delete Transition Attempt

## Scope

- Parent: `T110 Complete Authority Lifecycles`
- Security task: `T202C3 Account Credential And Cleanup Lifecycles`
- Attempt: exact `credential_delete` prepare, config commit, finalize, abort,
  recovery, retry, expiry, restart, immutable account/store version, and fault
  rollback lifecycle
- Production commit: `316dae0`
- Direct execution: the user requested strict serial continuation; no worker or
  concurrent authority edit owned this worktree

Cleanup challenge/claim/delete, credential-row deletion, schema, dependencies,
core transcripts, runtime, config, keyring, protocol, CLI, MCP, network, and
remote effects were excluded.

## TDD

The focused command was:

```text
cargo test -p kirje-store --test authority_registry \
  credential_delete_transition_lifecycle_is_exact \
  --all-features --locked
```

The valid RED exited 101 at the unsupported transition gate. An authorized
credential-delete prepare returned `authorization_context_stale`; its rejection
fingerprint proved zero grant, transition, credential, cleanup, version, event,
or clock changes. GREEN uses the same command and passes one database-backed
lifecycle test.

## State Matrix

| Path | Durable account | Durable store | Credential / cleanup | Result |
| --- | --- | --- | --- | --- |
| prepare | signed authorized/missing after generation and tuple, blocked | before config, blocked | historical credential retained; no cleanup | prepared |
| config commit | blocked after tuple plus one immutable account version | next config plus one immutable store version, blocked | unchanged | config committed |
| finalize | signed authorized/missing after projection, active | next config, active | unchanged | finalized |
| abort before config | exact authorized/bound predecessor tuple and receipt, active | before config, active | unchanged | aborted |
| unsafe observation | signed after tuple, blocked | observed pair, recovery required | unchanged | recovery required |
| exact retry | transition-scoped immutable projection | unchanged | no duplicate or deleted row | terminal or in-progress state replayed |
| expiry/restart/tamper | expiry is durable; create-set-delete history reopens; kind corruption fails closed | exact | unchanged | expired, Ready, or recovery required |

Injected prepare-transition, account-version, and finalize-account faults roll
back the complete authority transaction. Exact retries complete each affected
path without duplicate versions or identity mutation.

## Identity And Privacy Review

The grant target kind is `credential`; grant and challenge target bytes equal
the signed before credential ID. Core's sealed mutation keeps the credential
identity and binding material exact while advancing the account from
authorized/bound to authorized/missing. The complete before and after account
snapshots, store CAS, next generation, binding, and `active_locator_sha256`
remain covered by the immutable manifest and grant digest.

Authority stores no credential bytes or active-locator material. Prepare creates
neither a credential identity nor a cleanup row. Config commit creates one
account version and one store version using the permanently reserved credential
identity; supported code never updates or deletes its historical credential
row. The actual idempotent active-locator keyring deletion and crash ordering
remain runtime/config work outside this authority-only attempt.

The implementation adds no read, probe, copy, export, set, keyring, cleanup,
runtime, CLI, MCP, protocol, network, or external-call surface. The fixture
contains one synthetic public Ed25519 signature only. Privacy scans found no
private key, seed, proof, nonce, credential bytes, locator material, real
account data, endpoint, mailbox content, or provider data.

## Validation

- Focused credential-delete lifecycle: 1 passed, 0 failed.
- `cargo test -p kirje-store --all-features --locked`: unit 8,
  authorization 33, authority registry 40, schema 31, outbox 11, and doc tests
  passed.
- `cargo test -p kirje-store --no-default-features --locked`: 19 enabled unit
  and outbox tests passed; feature-gated authority suites compiled with zero
  selected tests.
- All-target, all-feature Clippy with `-D warnings`: passed.
- Format check, diff check, delivery artifact validation, integrity hashes, and
  privacy scan: passed.

## Review

Spec review passed the exact credential target, signed bound-to-missing
account/CAS/locator binding, retained credential identity, immutable after
versions, and no-cleanup boundary. Engineering review passed transaction
atomicity, predecessor receipt restoration, terminal replay, expiry event
identity, history cardinality, event order, restart validation, and corruption
recovery. QA covers the create-set-delete chain, success, abort, unsafe recovery,
expiry, restart, target mismatch, three injected fault points, and
transition-kind tamper.

No unresolved Critical, High, or Medium finding remains.

## Integrity

```text
60c185a6bac9e90d9e065e7f56075a6997b96ced432cf85d9d91d25afda7dbb5  crates/kirje-store/src/authority.rs
6480c17d2540e574531c613b91ab140eb6b4916f877586e81f81077e43c86564  crates/kirje-store/tests/authority_registry.rs
678899fee9cbed74625a56d159d1bf849874f2ae3cd6b4498b4ec048becd2efc  crates/kirje-store/tests/fixtures/authority/registry/account_credential_cleanup/signatures.txt
5d01739b89246a5f495a965e57e416eee9fd0b5016995add41c6edee7f3e970d  crates/kirje-store/src/authority/schema_v1.sql
596ddb0071ecb38b0b9429c5d91cdbb83abe6a25bc760cf9588b2764274ff850  Cargo.lock
```

## Result

`T202C3-A005_ACCEPTED`

The user explicitly accepted this attempt on 2026-08-30 at production commit
`316dae0`. Cleanup challenge/claim/delete remain fail-closed. `T202C3-A006` is
reserved for cleanup challenge issuance but Blocked on the missing normative
`tombstone_sha256` domain/transcript/derivation; T202C3 and T110 therefore stop
at the cleanup packet gate. Cleanup claim/delete remain a later attempt.
