# T202C3-A006 Credential Cleanup Challenge Attempt

## Status

`RETURNED_NEEDS_FIX`

- Production commit: `2241a946c399ba9c61e67e808a85f777c0d2b402`
- Parent: `T110 Complete Authority Lifecycles`
- Security task: `T202C3 Account Credential And Cleanup Lifecycles`
- Result: implementation and validation completed, but spec and QA review failed
- Acceptance: none; A006, T202C3, and T110 remain unaccepted

## Scope

The attempt changed only the packet-authorized paths:

- `crates/kirje-store/src/authority.rs`: 440 additions, 3 deletions
- `crates/kirje-store/tests/authority_registry.rs`: 687 additions, 19 deletions
- `crates/kirje-store/tests/fixtures/authority/registry/account_credential_cleanup/signatures.txt`:
  3 additions, 3 deletions

No schema, Cargo graph, lockfile, core transcript/type, runtime, keyring,
protocol, CLI, MCP, network, or external-provider file changed.

## TDD

The focused command was:

```text
cargo test -p kirje-store --test authority_registry \
  credential_cleanup_challenge_is_exact \
  --all-features --locked
```

RED exited 101. A valid cleanup challenge reached the intended unsupported
cleanup gate and returned `authorization_context_stale`. GREEN passed the same
single database-backed test after implementation. This RED proves the missing
capability was reached; it does not prove the review matrices listed below.

## Validation

- Focused cleanup challenge test: 1 passed, 0 failed, 44 filtered out.
- `cargo test -p kirje-store --all-features --locked`: unit 8,
  authorization 33, authority registry 45, schema 31, outbox 11, and doc tests
  passed with zero failure.
- `cargo test -p kirje-store --no-default-features --locked`: 8 unit and 11
  outbox tests passed; feature-gated suites selected zero tests.
- All-target, all-feature Clippy with `-D warnings`: passed.
- `cargo fmt --all --check`: passed.
- Worker diff/integrity/privacy checks: reported passed.

Passing commands establish build and regression health only. They do not close
the failed spec or QA findings.

## Reviews

| Review | Result | Critical | High | Medium | Low |
| --- | --- | ---: | ---: | ---: | ---: |
| Engineering | PASS | 0 | 0 | 0 | 0 |
| Spec compliance | FAIL | 0 | 1 | 1 | 0 |
| QA | FAIL | 0 | 0 | 4 | 0 |

Engineering found no blocking defect in the implemented mechanics, with one
residual coverage gap: a complete legacy cleanup flow remains unproved.

### High

Cleanup validation applies origin/locator/tombstone target checks before the
blocked/recovery eligibility checks required by the closed error precedence.
A mixed invalid target plus blocked store/account therefore returns
`credential_cleanup_invalid` instead of `account_update_conflict`; a mixed
invalid target plus recovery-required store returns
`credential_cleanup_invalid` instead of `owner_recovery_required`. This ordering
reveals whether private target material is valid. The fix must establish the
blocked/recovery result before target-validity distinctions and add mixed-state
zero-mutation tests.

### Medium

1. Legacy negatives are non-discriminating, and locator lower/upper bounds plus
   canonical field mutation coverage are incomplete.
2. Invalid-target expired-pending replacement does not actually exercise and
   prove complete expiry/successor/event/clock/entropy rollback.
3. Immutable public projections and exact clock, entropy, concurrency, pending
   reuse, and response-loss assertions are incomplete.
4. Historical later update/remove/recreation and durable restart-corruption
   matrices are incomplete.
5. `effect_observations`, external-call cardinality, and privacy/non-disclosure
   proof are incomplete.

## Integrity

```text
59d13b294ce2a0446ce9579391e49e05f96e62a9f079f5d10f39e5dc425ecc5d  crates/kirje-store/src/authority.rs
ad413f31952e1093e0174809bf4f37a23fffe97817fe65f1229fd3c281b62df2  crates/kirje-store/tests/authority_registry.rs
e5613c2fef5f0181dfe06ededec939b4e132fc4bbf1cd656dbdfbfe26f076608  crates/kirje-store/tests/fixtures/authority/registry/account_credential_cleanup/signatures.txt
5d01739b89246a5f495a965e57e416eee9fd0b5016995add41c6edee7f3e970d  crates/kirje-store/src/authority/schema_v1.sql
596ddb0071ecb38b0b9429c5d91cdbb83abe6a25bc760cf9588b2764274ff850  Cargo.lock
```

Evidence contains no locator transcript, credential, address, endpoint,
private key, proof, nonce, mailbox content, or provider response.

## Governance Integration Validation

- Ruby parsed all 32 `.ai-platform/**/*.yaml` files.
- 007 and 008 delivery artifact validators passed with zero errors or warnings.
- `git diff --check` passed.
- Scope inspection confirmed the integration diff contains governance/evidence
  files only and does not modify the returned production/test candidate.

## Stop And Resume Gate

Autonomous execution stopped after the High finding, and the recovery heartbeat
was paused under the user's High-stop condition. A006 is Returned/Needs Fix;
A007 and A008 remain non-executable.

## Explicit Resume And Fix Packet

The user explicitly resumed A006 repair on 2026-08-31 after receiving the
blocker summary and fixed the subsequent order as A006 repair and re-review,
A007 claim, A008 delete, then T202C3/T110 closure. This clears the resume gate
but does not retroactively accept candidate `2241a946` or its failed reviews.

`T202C3-A006-F001` is prepared as a governance-only fix candidate and is
reviewed at governance HEAD `c5732ac`. Engineering and QA passed with zero
finding; spec compliance failed C0/H1/M2/L0. F001 is
`blocked_needs_contract_clarification`. No production, test, or fixture edit
started, all permissions remain none, and the heartbeat is paused under the
High-stop condition.

The unresolved High is the assumption that caller-supplied common store/account
IDs are signed at challenge creation. Exact absent projection, pair mismatch,
unrelated blocked/recovery row, and closed public-projection behavior requires
contract clarification before private target validation. The two Mediums
require reachable replacement cases to be split and a safe parser-bound proof
to be chosen. No record attributes those unresolved choices to the user.

## F002 Contract Approval

The user explicitly approved the F002 contract revision and directed work to
continue on 2026-08-31. F002 treats common IDs as untrusted, defines the exact
absent/pair-mismatch/matched-state public projection before private step 7,
splits replacement proof into three reachable branches, and selects a private
numeric length classifier with numeric-only unit tests. This approval does not
accept implementation or claim a packet-review pass.

## F002 Packet Review Failure

`T202C3-A006-F002` was reviewed at governance HEAD
`38ca4273cb8e970f0e8f0da2e1eef3914dfa8a83`. QA passed C0/H0/M0/L0. Spec failed
C0/H0/M1/L0, and engineering failed C0/H1/M2/L0. No implementation attempt
started.

The High rejects F002's literal no-private-read wording because mandatory
global history validation already reads all cleanup/origin/locator graphs. A
successor must keep full step-2 validation and prove by call order and source
structure that no request-directed private lookup or request-dependent private
branch occurs after global validation. The Mediums record stale unreachable
invalid-target replacement text in `data-model.md`, the unresolved
`transition_id=None` error and preflight layer, and a stale F001 gate that must
be replaced by three independent reviews of the revised F002/F003 packet. No
record attributes those unresolved choices to the user.

F002 is `blocked_needs_contract_clarification`. Production, test, and fixture
permissions remain none. Autonomous execution stopped and the heartbeat is
paused under the user's High-stop condition. A007/A008 remain non-executable,
and T202C3/T110 remain unaccepted.

## F003 Delegated Clarification

On 2026-08-31 the user explicitly authorized the Delivery Orchestrator to
approve existing-boundary contract clarifications without requesting approval at
every node. Under that authority, the orchestrator approved F003's exact
clarification. This records delegated contract approval, not user review of the
unseen F003 packet.

F003 preserves complete request-independent global step-2 validation, which may
already stream every private graph. After that pass, no request-directed private
lookup or request-dependent private branch may occur before the closed public
pair classification, with mandatory source and call-order proof. Replacement is
limited to the three reachable branches. One pure exact-scope `authority.rs`
manifest preflight rejects `transition_id=None` as `AuthorizationMalformed`
before apply lock, file, database, or entropy work with zero I/O, mutation, or
entropy and no core change.

F003 packet review at governance HEAD `1d143cc` failed: engineering
C0/H1/M0/L0, spec C0/H0/M1/L0, and QA C0/H0/M1/L0. No implementation attempt
started and no pass token exists.

## F004 Delegated Clarification

Under the user's delegated existing-boundary authority, the orchestrator
approved F004's clarification on 2026-08-31. This is delegated approval, not a
claim that the user reviewed the unseen packet. Complete request-independent
global validation remains first; no request-directed pending/private lookup may
precede public-pair classification. Same-context expired pending plus later
blocked/recovery performs zero pending lookup-dependent interaction and leaves
predecessor state/event and both clocks unchanged, with zero entropy/successor/
grant/nonce/cleanup. Active eligible valid-target rollback uses
`OldChallengeExpiredState` and `OldChallengeExpiredEvent`. The packet enumerates
the full public-pair by six-private-invalid cross-product.

F004 packet review failed: engineering and spec found the cleanup-challenge
phase-ordering High; supporting findings covered the reachable proposed-state
matrix, effect scenarios, full validation commands, stale-authority audit, and
status wording. No aggregate severity counts were supplied, so this evidence
does not invent them. No implementation attempt or pass token exists.

## F005 Delegated Clarification

Under the user's delegated existing-boundary authority, the orchestrator
approved F005 on 2026-08-31. This is delegated approval, not user review of the
unseen artifact. F005 makes cleanup challenge precedence phase-specific:
preflight, lock/transaction, global validation, checked time without pending
access, public classification, private validation, pending lookup, reuse/
replacement, successor commit. It prohibits durable expiry before eligibility,
keeps claim/delete proof expiry separate, treats same-origin proposed as
corruption and unrelated proposed as reachable conflict, and completes the
six-table effect, full gate, and all-input authority audit requirements.

`T202C3-A006-F005` is `ready_for_f005_packet_review`. Production, test, and
fixture permissions remain none until three independent zero-finding F005
reviews pass and governance follow-up opens the exact scope. A007/A008 remain
non-executable; T202C3/T110 remain unaccepted.
