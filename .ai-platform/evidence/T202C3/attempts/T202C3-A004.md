# T202C3-A004 Credential Set Transition Attempt

## Scope

- Parent: `T110 Complete Authority Lifecycles`
- Security task: `T202C3 Account Credential And Cleanup Lifecycles`
- Attempt: exact `credential_set` prepare, config commit, finalize, abort,
  recovery, retry, expiry, restart, immutable account/store version, and fault
  rollback lifecycle
- Production commit: `1c6d7cb`
- Direct execution: the user requested strict serial continuation; no worker or
  concurrent authority edit owned this worktree

Credential delete, cleanup claim/delete, schema, dependencies, core
transcripts, runtime, config, keyring, protocol, CLI, MCP, network, and remote
effects were excluded.

## TDD

The focused command was:

```text
cargo test -p kirje-store --test authority_registry \
  credential_set_transition_lifecycle_is_exact \
  --all-features --locked
```

The discriminating RED exited 101 at the account-only transition gate. The
valid authorized credential-set prepare returned
`authorization_context_stale`; its rejection fingerprint proved zero grant,
transition, credential, cleanup, version, event, or clock changes. GREEN uses
the same command and passes one database-backed lifecycle test.

## State Matrix

| Path | Durable account | Durable store | Credential / cleanup | Result |
| --- | --- | --- | --- | --- |
| prepare | signed after generation and binding, blocked | before config, blocked | existing credential only; no cleanup | prepared |
| config commit | unchanged blocked after tuple plus one immutable account version | next config plus one immutable store version, blocked | existing credential only; no cleanup | config committed |
| finalize | authorized/bound after projection, active | next config, active | unchanged | finalized |
| abort before config | exact predecessor generation, tuple, and receipt, active | before config, active | unchanged | aborted |
| unsafe observation | signed after tuple, blocked | observed pair, recovery required | unchanged | recovery required |
| exact retry | transition-scoped immutable projection | unchanged | no duplicate row | terminal or in-progress state replayed |
| expiry/restart/tamper | expiry is durable; valid history reopens; kind corruption fails closed | exact | unchanged | expired, Ready, or recovery required |

Injected prepare-transition, account-version, and finalize-account faults roll
back the complete authority transaction. Exact retries complete each affected
path without duplicate versions or identities.

## Identity And Privacy Review

The grant target kind is `credential`, and both grant and challenge target
bytes must equal the signed after credential ID. The complete signed before and
after account snapshots, store CAS, next generation, binding, and
`active_locator_sha256` remain covered by the immutable manifest and grant
digest. Authority stores no credential bytes or active-locator material.

Prepare creates neither a credential identity nor a cleanup row. Config commit
creates one account version and one store version using the already registered
credential identity. Restart validation requires that credential to remain
owned by the exact account and store while preserving its original create
history. The implementation adds no read, probe, copy, export, set, delete,
cleanup, keyring, config, runtime, CLI, MCP, protocol, network, or external-call
surface.

The fixture contains one synthetic public Ed25519 signature only. Privacy scans
found no private key, seed, proof, nonce, credential bytes, locator material,
real account data, endpoint, mailbox content, or provider data.

## Validation

- Focused credential-set lifecycle: 1 passed, 0 failed after final replay
  assertions.
- `cargo test -p kirje-store --all-features --locked`: unit 8,
  authorization 33, authority registry 39, schema 31, outbox 11, and doc tests
  passed.
- `cargo test -p kirje-store --no-default-features --locked`: 19 enabled unit
  and outbox tests passed; feature-gated authority suites compiled with zero
  selected tests.
- All-target, all-feature Clippy with `-D warnings`: passed.
- Format check, diff check, delivery artifact validation, integrity hashes, and
  privacy scan: passed.

The package test included the complete production implementation and the full
credential-set lifecycle. Final focused and Clippy runs additionally validate
the terminal abort/recovery replay assertions added during review.

## Review

Spec review passed the exact credential target, signed account/CAS/locator
binding, existing credential identity, immutable after versions, authorized and
bound projection, and no-cleanup boundary. Engineering review passed transaction
atomicity, predecessor receipt restoration, terminal replay, expiry event
identity, history cardinality, event order, explicit credential-delete
rejection, restart validation, and corruption recovery. QA covers success,
abort, unsafe recovery, expiry, restart, target mismatch, three injected fault
points, and transition-kind tamper.

No unresolved Critical, High, or Medium finding remains.

## Integrity

```text
d4c4441c5edd2471b935eb37dd27be1c4ee059145bbe8a8eef2aafe786d51927  crates/kirje-store/src/authority.rs
5295983330486f8b48c5cf75bada42183c28e196620bab3a5501b3074e4dbd0a  crates/kirje-store/tests/authority_registry.rs
dcd04d612653ee5dc03c64e504cbb243d6909ba772b5b92c3eed9bbf03105740  crates/kirje-store/tests/fixtures/authority/registry/account_credential_cleanup/signatures.txt
5d01739b89246a5f495a965e57e416eee9fd0b5016995add41c6edee7f3e970d  crates/kirje-store/src/authority/schema_v1.sql
596ddb0071ecb38b0b9429c5d91cdbb83abe6a25bc760cf9588b2764274ff850  Cargo.lock
```

## Result

`T202C3-A004_REVIEW_PASS`

T202C3 and T110 remain Running. This checkpoint awaits explicit user
acceptance. Credential delete and cleanup claim/delete remain fail-closed and
no later attempt is Ready.
