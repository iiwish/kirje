# T202C3-A001 Challenge Issuance Attempt

## Scope

- Parent: `T110 Complete Authority Lifecycles`
- Security task: `T202C3 Account Credential And Cleanup Lifecycles`
- Attempt: exact challenge issuance only for `account_update`,
  `account_remove`, `credential_set`, and `credential_delete`
- Production commit: `daf22a0`
- Files: `crates/kirje-store/src/authority.rs` and
  `crates/kirje-store/tests/authority_registry.rs`

Transition execution, cleanup lifecycle mutation, remote effects, schema,
dependencies, core transcripts, runtime, keyring, protocol, CLI, MCP, and
network behavior were excluded.

## RED

The initial test harness compilation error was corrected before accepting RED.
The first discriminating execution was:

```text
cargo test -p kirje-store --test authority_registry \
  remaining_account_and_credential_challenges_are_exact \
  --all-features --locked
```

It exited 101 on the first valid `account_update` manifest because
`create_challenge` returned stable non-retryable
`authorization_context_stale`. No signature fixture or external capability was
involved.

## GREEN

The store now admits the four typed control actions only after:

- reparsing the sealed manifest and validating its intrinsic lifecycle shape;
- matching one active store location, generation, digest, and no active
  transition;
- matching one active account ID, display digest, generation, credential, and
  binding;
- rejecting reused transition, replacement credential, cleanup ID, or cleanup
  locator identities;
- preserving exact pending challenge reuse before later fresh-state checks.

All four actions remain effect-free. They write one challenge and one created
event only. Invalid intrinsic shapes, stale account generation, and a busy
store consume zero entropy and leave rows and the paired clock unchanged.

## Review

Spec compliance, engineering, and QA review found no unresolved Critical or
High issue. Review tightened provisional cleanup uniqueness, lifecycle reason
checks, explicit removed-account classification, exact created-event sequence
accounting, and the synthetic-data privacy scan before the final commit.

The final diff adds no address, endpoint, credential, secret, token, private
key, signature, proof, nonce, mailbox content, or provider data literal. It
changes no schema or dependency file and exposes no transition, cleanup,
keyring, protocol, or network call.

## Result

`T202C3-A001_REVIEW_PASS`

T202C3 and T110 remain Running. Account/credential transition execution and the
delete-only cleanup state machine are the next serial work; this attempt is not
authority-lifecycle acceptance.
