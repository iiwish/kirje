# T202C3-A003 Account Remove Transition Attempt

## Scope

- Parent: `T110 Complete Authority Lifecycles`
- Security task: `T202C3 Account Credential And Cleanup Lifecycles`
- Attempt: exact `account_remove` prepare, config commit, finalize, abort,
  recovery, retry, expiry, restart, removed-history, store-version, and
  cleanup-ready lifecycle
- Production commit: `703b5a1`
- Direct execution: the user requested strict serial continuation; no worker or
  concurrent authority edit owned this worktree

Credential set/delete, cleanup claim/delete, account purge, schema,
dependencies, core transcripts, runtime, config, keyring, protocol, CLI, MCP,
network, and remote effects were excluded.

## TDD

The focused command was:

```text
cargo test -p kirje-store --test authority_registry \
  account_remove_transition_lifecycle_is_exact \
  --all-features --locked
```

The first RED exited 101 at the request builder because cleanup reservations
were still update-only. After admitting the already sealed remove cleanup
descriptor at that input boundary, the second RED exited 101 at the intended
create/update-only runtime gate with `authorization_context_stale`. Its
rejection fingerprint proved zero grant, transition, cleanup, event, or clock
writes. GREEN uses the same command and passes one database-backed lifecycle
test.

## State Matrix

| Path | Durable account | Durable store | Cleanup | Result |
| --- | --- | --- | --- | --- |
| prepare | exact predecessor generation and tuple, blocked | before config, blocked | provisional | prepared |
| config commit | unchanged predecessor tuple, blocked | next config plus immutable store version, blocked | provisional | config committed |
| finalize | same identity and generation, removed with `removed_at` | next config, active | ready plus exact event | finalized |
| abort before config | exact predecessor tuple and receipt, active | before config, active | provisional | aborted |
| unsafe observation | predecessor tuple, blocked | observed pair, recovery required | provisional | recovery required |
| exact retry | transition-scoped immutable projection | unchanged | exact private bytes rechecked | no duplicate writes |
| expiry/restart/tamper | expiry is durable; valid history reopens; material corruption fails closed | exact | exact | expired, Ready, or recovery required |

Config commit creates no account version and no credential identity. Removal
after a finalized update preserves generation two and restarts with the exact
create-update-remove history.

## Identity And Privacy Review

Finalize releases only the active display slot. A fresh account with a fresh
credential can reuse that display; the removed account ID and historical
credential ID remain permanently reserved and fail before entropy or writes.
All account, credential, grant, receipt, transition, version, and cleanup rows
remain immutable.

Cleanup material stays inside the bounded non-Debug reservation and private
SQLite row. The implementation adds only `provisional -> ready`; it adds no
read, probe, copy, export, set, claim, delete, purge, runtime, keyring, CLI,
MCP, protocol, or network capability. The committed fixture contains synthetic
public Ed25519 signatures only and no private key, seed, proof, nonce, real
account data, endpoint, mailbox content, or provider data.

## Review

Spec review passed exact signed before/CAS/cleanup binding, absent after
snapshot, store-only after versioning, removed-history preservation, and
finalize-only display release. Engineering review passed transaction rollback,
predecessor receipt restoration, terminal retry, event order, successor
history, expiry, restart, and corruption checks. QA covered direct removal,
removal after update, abort, unsafe recovery, expiry, replay, display reuse,
old identity rejection, injected faults, and cleanup tamper.

Review added the create-update-remove chain and explicit historical credential
reuse negative. No unresolved Critical, High, or Medium finding remains.

## Result

`T202C3-A003_REVIEW_PASS`

T202C3 and T110 remain Running. This checkpoint requires explicit user
acceptance before the credential-set transition packet is prepared.
