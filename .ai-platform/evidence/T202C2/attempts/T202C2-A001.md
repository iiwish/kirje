# T202C2-A001 Interrupted Attempt

## Metadata

- Task: `T202C2 Account Creation Transition`
- Attempt: `T202C2-A001`
- Status: Interrupted_After_Validation
- Branch: `codex/v1-roadmap-governance`
- Baseline HEAD: `ab9f059fa21100808d279bfa9d441565f8418992`
- Started: 2026-08-30 15:25:05 +0800
- Interrupted: 2026-08-30 16:49:24 +0800
- Source thread: `01a0464e-c372-7140-b475-fecdc01065eb`

## Preserved Changes

- `crates/kirje-store/src/authority.rs`
- `crates/kirje-store/tests/authority_registry.rs`
- `crates/kirje-store/tests/fixtures/authority/registry/account_create/vectors.txt`

The tracked diff contains 5,049 insertions and 295 deletions. The new fixture
contains 29 lines. No schema, dependency, runtime, CLI, MCP, protocol, keyring,
network, or governance file was changed by the worker.

## RED Evidence

The initial focused command was:

```bash
cargo test -p kirje-store --test authority_registry --all-features --locked
```

It exited 101 because the store did not expose the account-transition types,
methods, or `registry_parent_preflight` counter required by T202C2. The missing
surface included:

- `AccountTransitionKind`
- `AccountTransitionObservationRequest`
- `AccountTransitionProjection`
- `AccountTransitionState`
- `PrepareAccountTransitionRequest`
- `RegisteredAccountState`
- `RegisteredStoreTransitionState`
- `prepare_account_transition`
- `mark_config_committed`
- `finalize_account_transition`
- `abort_transition`
- `mark_transition_recovery_required`

A discriminating behavioral RED then failed with
`UnsupportedCapability: account transition preparation is not implemented`
for `account_create_prepare_commit_finalize_and_enrollment_retry_are_exact`.

Additional failing runs exposed restart-corruption, query-count, expiry,
concurrency, and terminal-retry defects. They were retained until their focused
tests passed.

## GREEN Evidence

The following commands completed successfully before interruption:

- `cargo fmt --all && cargo test -p kirje-store --test authority_registry --all-features --locked`
  - 32 passed, 0 failed.
- `cargo fmt --all --check`
- `cargo clippy -p kirje-store --all-targets --all-features --locked -- -D warnings`
- `cargo test -p kirje-store --test authority_authorization --all-features --locked`
  - 33 passed, 0 failed.
- `cargo test -p kirje-store --test authority_schema --all-features --locked`
  - 31 passed, 0 failed.
- `cargo test -p kirje-store --no-default-features --locked`
- `cargo test -p kirje-store --all-features --locked`
- `cargo +1.88.0 test -p kirje-store --all-features --locked`
- `cargo test --workspace --all-features --locked`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo build --workspace --all-features --locked`
- `cargo deny check licenses`
- `cargo deny check bans`
- `cargo deny check sources`
- `git diff --check`
- Canonical Authority SQLite v1 schema hash check.
- Changed-scope and full-tree secret/private-material scans.
- No-log, no-`unsafe`, no-network, no-runtime/protocol/keyring store scan.
- O(1) restart-validator source scan and indexed query-path inspection.

## Failed Gate

`cargo deny check advisories` failed on yanked transitive dependency
`chacha20 0.10.1` through:

```text
io-imap -> imap-codec -> imap-types -> rand -> chacha20
```

`Cargo.lock` is unchanged from baseline HEAD, so T202C2 did not introduce this
finding. The accelerated v1 work graph assigns remediation to T111 and blocks
Security Alpha acceptance until the complete dependency gate passes.

## Content Hashes

```text
77c3e62681bb33f10fae6ca373d607b4a8151a9f4e5f4ded5b69e5cac730d7f4  crates/kirje-store/src/authority.rs
0dacb159e5aa70e3c81f760e1f5cdf27789e3c93ef175cf9f00bf312f2d2ca2b  crates/kirje-store/tests/authority_registry.rs
c5af4427045606c90b8d4f5fcd634290b383a83f45a61248227143582b86b0e5  crates/kirje-store/tests/fixtures/authority/registry/account_create/vectors.txt
5d01739b89246a5f495a965e57e416eee9fd0b5016995add41c6edee7f3e970d  crates/kirje-store/src/authority/schema_v1.sql
80673cbb8a2975c5ee0729a365cb8f49483d3db135879bafc9d792036bd6c3a2  crates/kirje-store/src/lib.rs
596ddb0071ecb38b0b9429c5d91cdbb83abe6a25bc760cf9588b2764274ff850  Cargo.lock
```

Successful same-attempt validation may be reused only while every relevant hash
remains exact. A scoped review fix invalidates the affected evidence and
requires fresh focused and changed-crate validation.

## Review State

Implementation evidence exists, but spec compliance, adversarial engineering
review, evidence integration, commit, and user acceptance remain incomplete.
The task is therefore Needs_Review, not Accepted.

## Residual Risk

- The account-create diff is large and has not received independent code review.
- Full outputs are retained in the source task history rather than committed
  verbatim because they may be large; this record captures the exact commands,
  exit state, hashes, and supported claims.
- The yanked dependency blocks Security Alpha, even though it is outside the
  T202C2 change scope.
