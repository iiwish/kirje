# T023 Evidence: MCP Contract

Implemented typed draft and mailbox-operation tools over the shared runtime.
MCP exposes plan/status/list/apply/audit capabilities but no approval or secret
operation. MCP stdio and router tests cover tool count, schemas, annotations,
and protocol-clean output.
