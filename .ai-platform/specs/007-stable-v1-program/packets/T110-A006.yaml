schema_version: 1
packet_id: 007-T110-T202C3-A006-F002
task_id: T110
feature_id: 007-stable-v1-program
mode: delegated_test_first_execution
adapter: codex_worker
status: ready_for_f002_packet_review

governance_inputs:
  constitution: .ai-platform/memory/constitution.md
  product_contract: .ai-platform/specs/007-stable-v1-program/spec.md
  requirements_checklist: .ai-platform/specs/007-stable-v1-program/checklists/requirements.md
  program_plan: .ai-platform/specs/007-stable-v1-program/plan.md
  work_graph: .ai-platform/specs/007-stable-v1-program/tasks.md
  analysis: .ai-platform/specs/007-stable-v1-program/analysis.md
  security_spec: .ai-platform/specs/008-security-baseline/spec.md
  security_plan: .ai-platform/specs/008-security-baseline/plan.md
  security_analysis: .ai-platform/specs/008-security-baseline/analysis.md
  security_work_graph: .ai-platform/specs/008-security-baseline/tasks.md
  authority_contract: .ai-platform/specs/008-security-baseline/contracts/authority-store.md
  authorization_contract: .ai-platform/specs/008-security-baseline/contracts/authorization.md
  account_contract: .ai-platform/specs/008-security-baseline/contracts/account-config-v2.md
  data_model: .ai-platform/specs/008-security-baseline/data-model.md
  predecessor_evidence: .ai-platform/evidence/T202C3/attempts/T202C3-A005.md
  historical_packet_review_evidence: .ai-platform/evidence/T202C3/attempts/T202C3-A006-packet-review.md
  failed_f001_packet_review_evidence: .ai-platform/evidence/T202C3/attempts/T202C3-A006-F001-packet-review.md
  f002_packet_review_evidence: .ai-platform/evidence/T202C3/attempts/T202C3-A006-F002-packet-review.md
  approval_gates:
    T202C3_A005: accepted by user on 2026-08-30 at production commit 316dae0
    cleanup_contract: approved by user on 2026-08-30
    material_architecture: >-
      Approved by the orchestrator on 2026-08-31 under the user's standing
      delegated authority; the approved decision specifies an unpublished
      kirje-credential crate directly depended on only by kirje-store and
      supersedes the flawed shared runtime/pluggable deletion formulation. It
      approves future architecture, not implementation and not personal user
      review of this packet.
    delegated_acceptance: >-
      The orchestrator may accept later artifacts only after required TDD,
      validation, evidence, and three reviews; no record may claim the user
      personally reviewed unseen work.
    historical_packet_review: >-
      First review failed with three High findings. Repeat review accepted the
      binding and clock repairs but found one High in the public low-level
      deletion surface. Final QA of the store-only repair found one Medium in
      the literal call-site scan. At HEAD 3533054, final independent spec,
      engineering, and QA passes each returned PASS with zero Critical, High,
      Medium, or Low finding and authorized exact A006 production/test scopes.
    historical_packet_review_token: T202C3_A006_PACKET_REVIEW_PASS
    execution_review: >-
      Candidate commit 2241a946c399ba9c61e67e808a85f777c0d2b402 was
      returned: engineering PASS C0/H0/M0/L0 with a legacy full-flow residual;
      spec FAIL C0/H1/M1/L0; QA FAIL C0/H0/M4/L0.
    explicit_resume: >-
      The user explicitly resumed A006 repair on 2026-08-31 and fixed the
      subsequent path as A006 repair and re-review, then A007 claim, then A008
      delete, then closure of T202C3/T110. This clears only the prior resume
      gate; it does not authorize production, test, or fixture edits before
      this F001 fix packet passes independent review.
    f001_packet_review: >-
      FAILED at governance HEAD c5732acb78409e20ba8f74a8a0c1f31dcbd19f69:
      engineering PASS C0/H0/M0/L0; QA PASS C0/H0/M0/L0; spec FAIL
      C0/H1/M2/L0. No pass token or execution authorization exists.
    f002_contract_revision: >-
      Explicitly approved by the user on 2026-08-31 with direction to continue.
      This approves the exact untrusted-ID public projection, reachable
      replacement branches, and private numeric length-classifier test seam in
      this packet. It does not approve implementation or claim review of F002.
    f002_packet_review: pending; three independent PASS reviews are required

work_unit:
  title: Issue an exact effect-free credential-cleanup challenge
  attempt: T202C3-A006-F002
  parent: T110 Complete Authority Lifecycles
  security_task: T202C3 Account Credential And Cleanup Lifecycles
  goal: >-
    Harden CredentialCleanupReservation construction and account-transition
    prepare so only the canonical private locator transcript for the signed
    realm and historical-before origin can be inserted, then implement exact
    effect-free credential_cleanup challenge issuance, replacement, concurrency,
    restart, and pending reuse from one transition-bound ready tombstone. Add no
    transition state-machine change, grant use, claim, delete permit, deletion backend,
    keyring, runtime, or external-effect capability.
  depends_on:
    - T202C3-A005 Accepted at production commit 316dae0
    - approved cleanup security contract in 008 canonical artifacts
    - historical T202C3_A006_PACKET_REVIEW_PASS at governance HEAD 3533054
    - explicit user resume on 2026-08-31 after the A006 blocker summary
    - explicit user approval of the A006 F002 contract revision on 2026-08-31
  blocks:
    - T202C3-A007 cleanup claim packetization
    - T202C3 completion
  parallel: false
  conflicts_with:
    - all concurrent authority registry, transition, cleanup, event, or restart validation work

codebase_context:
  branch: codex/v1-roadmap-governance
  baseline_head: 2153f4b3699692970c95e224e45f2caa2e05be4c
  reviewed_governance_head: 3533054
  existing_surfaces:
    - sealed CredentialCleanupManifest and canonical authorization TLV helpers in kirje-core
    - transition-bound private cleanup reservations and cleanup-ready event 15 from accepted A002/A003
    - canonical credential_cleanup states and authority event codes 16/17 in unchanged Authority SQLite v1
  implementation_rule: >-
    Existing code supplies storage and parsing mechanics but cannot override the
    canonical security semantics in contract_snapshot.

  review_failures_addressed:
    - cleanup common binding is historical origin-before, while account remove and credential delete retain current signed-before binding
    - exact pending reuse and claimed recovery may mutate only the paired authority clock high-water
    - the unpublished kirje-credential crate is a direct dependency of kirje-store only; no public trait/plugin surface lets runtime bypass authority
    - A008 uses an exhaustive AST allowlist rather than a bypassable literal sole-call scan
    - F002 treats cleanup common IDs as untrusted and closes absent, pair-mismatch, matched blocked/recovery, unrelated-pair, and active/removed public projection before private step 7
    - F002 splits same-context later-eligibility failure, different-context invalid target, and persisted-corruption replacement branches
    - F002 proves generic length gates through one private numeric classifier and keeps closed-form locator bytes in authority_registry.rs only

execution_result:
  candidate_commit: 2241a946c399ba9c61e67e808a85f777c0d2b402
  status: returned_needs_fix
  blocking_high: >-
    Cleanup validates origin/locator/tombstone before blocked/recovery
    eligibility, so a mixed invalid target returns credential_cleanup_invalid
    instead of account_update_conflict or owner_recovery_required and leaks
    target validity.
  medium_findings:
    - legacy negatives and locator bounds/mutation coverage are not discriminating or complete
    - invalid-target expired-pending replacement rollback is not actually exercised
    - immutable projection, clock, entropy, concurrency, pending-reuse, and response-loss assertions are incomplete
    - historical later update/remove/recreation and durable restart-corruption matrices are incomplete
    - effect_observations, external-call cardinality, and privacy proof are incomplete
  f001_packet_review: engineering and QA PASS C0/H0/M0/L0; spec FAIL C0/H1/M2/L0
  f002_contract_revision: explicitly approved by user on 2026-08-31; no implementation or packet-review pass yet
  resume_gate: satisfied by explicit user resume on 2026-08-31
  contract_gate: satisfied by explicit user approval of F002 on 2026-08-31
  next_gate: three independent F002 packet-review passes; no implementation attempt started

contract_snapshot:
  locator:
    domain: KIRJE-DELETE-ONLY-LOCATOR-V1 followed by NUL
    fields:
      - 0x0001 locator_kind u8
      - 0x0002 service UTF-8, 1..128 bytes and characters
      - 0x0003 username UTF-8, 1..1024 bytes and characters
    total_bound: 1..4096 bytes
    active_v2: exact v2 service and lowercase digest username from realm/store/account/historical credential/historical binding
    legacy_v1: exact legacy service plus origin-before historical display ID
    digest: SHA-256 of the complete canonical transcript
  tombstone:
    domain: KIRJE-CREDENTIAL-CLEANUP-TOMBSTONE-V1 followed by NUL
    ordered_fields:
      - 0x0001 realm_id BLOB32
      - 0x0002 cleanup_id UUID16
      - 0x0003 transition_id UUID16
      - 0x0004 transition_sha256 BLOB32
      - 0x0005 origin_manifest_sha256 BLOB32
      - 0x0006 store_id UUID16
      - 0x0007 account_id UUID16
      - 0x0008 historical_account_generation u64be
      - 0x0009 historical_credential_id UUID16
      - 0x000a historical_binding_sha256 BLOB32
      - 0x000b locator_kind u8
      - 0x000c locator_sha256 BLOB32
      - 0x000d created_at i64be
      - 0x000e origin_expected_state u8, exact 0x01 provisional
    exclusions:
      - raw locator material
      - mutable cleanup state, claim grant, and deleted time
    derivation: >-
      Rederive from realm meta, cleanup row, finalized origin transition and
      transition digest, origin grant/challenge manifest and its exactly-one
      provisional descriptor, plus the immutable origin-before account/version.
  eligibility:
    - Cleanup manifest transition_id is present and equals the row/origin transition; None is rejected.
    - Persisted NULL transition is owner_recovery_required corruption; schema and core transcript bytes do not change.
    - Origin is finalized account_update or account_remove and created_at equals prepared_at.
    - Store is active and historical account registry projection is active or removed.
    - Blocked store/account returns account_update_conflict; recovery store returns owner_recovery_required.
    - Manifest expected state is ready and common binding is origin-before historical binding.
    - Later account update/remove/recreation does not rebind the origin.
    - >-
      Closed precedence step 2 remains authoritative for persisted schema,
      anchor, history, transcript, or event corruption, including a persisted
      NULL transition, and returns owner_recovery_required without a private
      target distinction.
    - >-
      ActionManifest is caller-supplied and unproved at challenge creation.
      Common store/account IDs are untrusted typed request values, not a signed
      subject. After global integrity validation, absent store, absent account,
      or account.store_id mismatch returns credential_cleanup_invalid without
      reading cleanup, origin, locator, or tombstone state.
    - >-
      For an existing matched pair, recovery_required store returns
      owner_recovery_required; blocked store or blocked/proposed account returns
      account_update_conflict before private target validity. This deliberately
      applies to an unrelated existing matched pair. Active store plus active or
      removed account alone proceeds to private step-7 validation.
  reservation_prepare:
    - CredentialCleanupReservation::new parses one complete bounded canonical locator transcript, checks caller kind, and derives its digest.
    - Before row insertion, prepare rederives exact kind/service/username/digest from authority realm and signed historical-before origin.
    - Malformed constructor input is invalid_input; canonical wrong-origin prepare is authorization_context_stale with zero mutation.
    - No prepare, commit, finalize, abort, recovery, account, store, event, or clock state-machine rule changes.
  challenge:
    - Issuance and exact pending reuse create zero grant uses, cleanup mutations, effects, invocations, keyring calls, or external calls.
    - Exact pending reuse consumes no entropy, returns the same challenge, appends no event, changes no lifecycle/event timestamp, and may advance only the paired authority clock.
    - Valid expired-pending replacement records one predecessor event 5 before one successor event 3 and creates exactly one fresh grant UUID and nonce.
    - Same-context replacement can fail after later blocked/recovery eligibility and rolls back expiry, successor, events, clock, and entropy; a different-context invalid target has zero predecessor interaction.
    - Public projection, errors, logs, events, fixtures, and evidence omit locator material and private fields.

f002_contract:
  public_projection_precedence:
    - >-
      Run complete global schema, anchor, history, transcript, and event
      validation first. Persisted corruption remains precedence step 2 and
      returns owner_recovery_required.
    - >-
      Treat common store/account IDs only as bounded untrusted typed request
      data. Absent store, absent account, or persisted account.store_id unequal
      to the request store ID returns credential_cleanup_invalid without a
      cleanup row, origin transition, locator, tombstone, or other private
      target-validity read.
    - >-
      For an existing matched pair, recovery_required store returns
      owner_recovery_required; blocked store or blocked/proposed account returns
      account_update_conflict. An unrelated matched blocked/recovery pair
      deliberately returns the same public result and never distinguishes
      cleanup validity. Active store plus active/removed account proceeds to
      private step-7 cleanup/origin/locator/tombstone validation.
    - >-
      The first RED must cover known, absent, pair-mismatched, and unrelated
      blocked/recovery public rows crossed with invalid private targets. Every
      case must prove no target-validity distinction, durable mutation, or
      entropy consumption.
  locator_and_legacy:
    - >-
      Every legacy negative invokes the legacy_v1 caller kind, so a rejection
      cannot pass by accidentally selecting active_v2. The matrix covers the
      domain, exact kind, exact services, lowercase username shape, digest,
      unrecognized/duplicate/out-of-order tags, trailing bytes, declared-length
      mismatch, malformed or noncanonical UTF-8, and embedded NUL.
    - >-
      Generic service, username, and total-length gates use one private numeric
      length-classification helper in authority.rs. Its #[cfg(test)] unit tests
      pass numeric lengths and expected classes only: service byte/character
      0, 1, 128, 129; username byte/character 0, 1, 1024, 1025; and total 0, 1,
      4096, 4097 plus the greatest constructible canonical total. No public/
      test-support API is added and no locator transcript/test-vector bytes are
      added to authority.rs. Closed-form byte tests remain only in
      authority_registry.rs.
    - >-
      One transition-bound legacy_v1 flow succeeds through reservation,
      prepare, finalize, and exact cleanup challenge issuance from the
      historical origin-before display ID.
    - >-
      authority_registry.rs retains canonical locator and exact 14-tag tombstone
      byte/digest goldens, every one-field closed-form mutation, malformed
      active_v2/legacy_v1 constructor cases, and canonical wrong-origin prepare
      with zero durable mutation and entropy.
  replacement_reuse_and_races:
    - >-
      The matrix separates three reachable branches: same-context
      expired pending followed by later blocked/recovery eligibility;
      different-context invalid target with zero predecessor interaction; and
      persisted target/history corruption returning precedence-step-2
      owner_recovery_required. It never describes an unreachable
      same-context invalid-target mutation as an executable test. On failed
      same-context replacement, a durably expired predecessor stays expired and
      tentative expiry of a pending predecessor rolls back to pending; no new
      predecessor event, successor, authority clock field, or entropy commits.
    - >-
      Valid expired replacement proves fresh grant and nonce, exactly one
      predecessor event 5 followed by exactly one successor event 3, unchanged
      cleanup, and no grant use or effect row.
    - >-
      Exact pending reuse, restart reuse, and every concurrent loser compare
      the complete immutable challenge projection byte-for-byte, consume no
      entropy, append no event, change no lifecycle/event timestamp, and may
      change only authority_meta.last_observed_at and authority_meta.updated_at
      as the paired clock high-water. Exactly one concurrent winner owns the
      new grant, nonce, and event.
    - >-
      Cleanup-specific response loss replays the exact committed request after
      reopen and returns the same immutable challenge with the same clock-only
      allowance and no second lifecycle interval.
  history_and_restart:
    - >-
      Finalized later account update, finalized later account remove, and
      same-display account recreation each prove that historical origin-before
      account, generation, credential, binding, and legacy display ownership do
      not rebind.
    - >-
      Reopen must fail owner_recovery_required for duplicate or missing cleanup
      descriptor, nonfinal or wrong-kind origin, historical account/generation/
      credential/binding tuple drift, created_at/prepared_at drift, and
      impossible ready-state transition, digest, claim, or deleted fields.
  effects_and_privacy:
    - >-
      First issuance, exact reuse, response-loss replay, failed replacement,
      and concurrent losers prove zero delta in grant_uses, challenge_effects,
      remote_effects, effect_claims, effect_invocations, and
      effect_observations, plus zero cleanup mutation and zero external-call
      count.
    - >-
      Static/source inspection proves A006 adds no keyring, credential backend,
      delete permit, delete-only call, runtime, network, or other external
      capability. Errors, events, fixtures, diagnostics, Debug/serialization,
      evidence, and public projections are scanned for locator service,
      username, transcript bytes, origin private fields, credentials, and raw
      target material.

later_attempt_outlines:
  T202C3_A007:
    status: non_executable
    owns:
      - new unpublished crates/kirje-credential workspace member, root/store-only direct dependency entries, and opaque non-Clone/non-Debug/non-Serialize DeleteOnlyLocator
      - checked locator construction exposed only as required by the sole direct Cargo consumer; no janitor trait and no store re-export
      - store-private fake deletion hook, store-owned CleanupDeletePermit, and combined high-level store method foundation; runtime has no low-level access
      - atomic grant use plus ready-to-claimed transition and exact/changed retry, expiry, response loss, and concurrency
      - opaque non-Clone, non-Debug, non-Serialize, no-export CleanupDeletePermit holding the apply lock
      - event 16 source runtime, related grant, use_sha256 context, same receipt/time, adjacent after event 7
      - exact claimed recovery changes only the paired authority clock after lock reacquisition
  T202C3_A008:
    status: non_executable
    owns:
      - concrete low-level keyring delete function returning `Result<(), MailError>` and the sole production call in the combined store method
      - combined consuming delete boundary with no caller mark_deleted; authority open/read/validation paths make zero keyring calls
      - deleted/no-entry Ok(()) indistinguishability, backend-failure claimed state, crash recovery, and terminal no-recall retry
      - exact private module credential_cleanup_delete_adapter and combined method AuthorityStore::apply_credential_cleanup_delete; only this method may mention DeleteOnlyLocator or call kirje_credential::delete_only exactly once
      - dedicated AST test parsing every kirje-store production Rust file and rejecting low-level imports, aliases, wildcards, re-exports, macro/function-pointer/indirect bindings, constructor/type/API references, and calls outside the exact adapter method
      - Cargo metadata/tree direct-dependency allowlist, runtime compile-fail no-dependency fixture, and store no-re-export check
      - if necessary, one scoped kirje-store test-only Rust parser dev dependency plus its reviewed Cargo.lock change and proof it is absent from the production dependency tree
      - event 17 source runtime, same grant/use/receipt, exactly once after event 16
  integration_owner: >-
    T204 owns migration/removal of legacy runtime SecretStore paths, runtime/CLI
    wiring only to the high-level store cleanup API, and end-to-end integration.
    Runtime must not directly depend on, import, receive, or re-export
    kirje-credential or locator material.

execution_constraints:
  production_permission: none_pending_three_independent_f002_packet_reviews
  test_permission: none_pending_three_independent_f002_packet_reviews
  future_allowed_files:
    - crates/kirje-store/src/authority.rs
    - crates/kirje-store/tests/authority_registry.rs
    - crates/kirje-store/tests/fixtures/authority/registry/account_credential_cleanup/**
  forbidden_changes:
    - authority schema, application ID, user version, Cargo graph, or core transcript bytes/types
    - cleanup claim/delete, permit, deletion backend, runtime, config, keyring, protocol, CLI, MCP, network, or remote-effect behavior
    - transition state-machine behavior outside locator input hardening before cleanup insertion
  write_rules:
    - No production, test, or fixture edit may begin until all three independent F002 packet reviews pass and a governance follow-up opens the exact permissions.
    - The returned attempt scope is historical evidence, not current write permission.
    - Evidence, SSOT, packet, and status files remain orchestrator-owned.
    - >-
      The authority.rs length helper is private; its #[cfg(test)] tests use only
      numeric lengths/classes, add no public or test-support API, and add no
      locator transcript/test-vector bytes.
    - Deterministic synthetic non-real locator transcript bytes may appear only in authority_registry.rs for canonical byte goldens.
    - Signature fixtures may change only if deterministic synthetic public signatures truly change; they contain no locator transcript. Evidence/output/logs contain only vector IDs, digests, counts, and pass/fail results.
    - Real/private locator bytes remain prohibited in every committed file; private keys, seeds, real proofs/nonces, credentials, and real account data are also prohibited.
    - Do not mark A007, A008, T202C3, or T110 Ready or Accepted from this packet.

tdd_plan:
  ordering: >-
    F002 remains non-executable until all three independent packet reviews pass.
    After authorization, the public-projection RED is first. Each retained
    initial/F001 review category receives its discriminating pre-change test;
    real defects require a captured RED before corresponding production change,
    while already-correct behavior receives honest test-only proof.
  red:
    - Run the known/absent/pair-mismatch/unrelated-row public-projection matrix before every production edit and retain exact zero-mutation/entropy snapshots.
    - Run correct-kind legacy, private numeric-only length-classifier unit tests, closed-form registry locator/tombstone goldens and mutations, malformed reservation/wrong-origin prepare, and the successful transition-bound legacy flow.
    - Run the three reachable replacement branches and valid replacement freshness/event-cardinality matrices.
    - Run complete immutable-projection, paired-clock-only reuse/loser, concurrent issuance, restart, and cleanup response-loss matrices.
    - Run later-history non-rebinding plus descriptor/origin/tuple/timestamp/ready-state durable restart-corruption matrices.
    - Run all-effect-table/grant-use/external-call cardinality and privacy/capability source scans.
  green:
    - Parse the canonical locator at reservation and rederive/compare kind, service, username, and digest from realm plus signed historical-before origin before row insertion.
    - Rederive the tombstone from immutable origin history with bounded streaming validation.
    - Admit only exact ready challenge issuance and pending reuse with zero authorization effect.
    - Extend restart validation for the A006 challenge graph without admitting claim/delete rows or events 16/17.
  refactor:
    - Share canonical TLV helpers only when the private locator/tombstone bounds and action-specific validation stay explicit.

validation_loop:
  focused_red_green:
    - cargo test -p kirje-store --test authority_registry credential_cleanup_challenge_is_exact --all-features --locked
  complete:
    - cargo test -p kirje-store --test authority_registry --all-features --locked
    - cargo test -p kirje-store --all-features --locked
    - cargo test -p kirje-store --no-default-features --locked
    - cargo clippy -p kirje-store --all-targets --all-features --locked -- -D warnings
    - cargo fmt --all --check
  integrity:
    - git diff --check
    - shasum -a 256 crates/kirje-store/src/authority/schema_v1.sql Cargo.lock
    - python3 /Users/iiwish/.codex/skills/ai-delivery-governor/scripts/validate_delivery_artifacts.py --root /Users/iiwish/self/kirje --feature-id 007-stable-v1-program
    - python3 /Users/iiwish/.codex/skills/ai-delivery-governor/scripts/validate_delivery_artifacts.py --root /Users/iiwish/self/kirje --feature-id 008-security-baseline

evidence_contract:
  attempt_path: .ai-platform/evidence/T202C3/attempts/T202C3-A006-F002.md
  packet_review_path: .ai-platform/evidence/T202C3/attempts/T202C3-A006-F002-packet-review.md
  summary_path: .ai-platform/evidence/T202C3/summary.md
  test_results_path: .ai-platform/evidence/T202C3/test-results.md
  required:
    - exact RED/GREEN commands and results
    - clarified known/absent/pair-mismatch/unrelated-row public-projection RED, persisted-corruption control, and zero-target-distinction/mutation/entropy snapshots
    - correct-kind legacy success/negatives, private numeric-only length-classifier unit proof, and closed-form locator/tombstone goldens and mutations confined to authority_registry.rs
    - three reachable replacement branches plus valid replacement, complete immutable projection, paired-clock-only reuse/loser, concurrency, restart, entropy/event, and cleanup response-loss matrices
    - historical later-update/remove/recreation non-rebinding and descriptor/origin/tuple/timestamp/ready-state restart-corruption matrices
    - zero grant-use and zero row delta for every effect table including effect_observations, plus zero cleanup/external-call and privacy/capability source proof
    - schema and Cargo.lock unchanged hashes
    - spec, engineering, and QA reviews with residual risk
  prohibited:
    - raw locator transcript bytes; evidence names synthetic vector IDs and digests only
    - credentials, addresses, endpoints, private keys, seeds, real proofs/nonces, mailbox content, or real provider data

review_contract:
  historical_review: T202C3_A006_PACKET_REVIEW_PASS at governance HEAD 3533054 authorized only the returned candidate
  failed_f001_packet_review: at c5732acb78409e20ba8f74a8a0c1f31dcbd19f69 engineering and QA passed, spec failed C0/H1/M2/L0
  required_f002_packet_review: pending; three independent PASS reviews with zero findings are required before any permission opens
  production_review: failed at candidate commit 2241a946c399ba9c61e67e808a85f777c0d2b402
  pass_1_spec_compliance:
    - global corruption first; untrusted common IDs; exact absent/pair-mismatch/unrelated-row public classification before private step 7; reachable replacement branches; private numeric parser proof; and no core/schema change
  pass_2_engineering:
    - implementable non-disclosing public classification, private numeric-only classifier with no public/test-support API, reservation/prepare canonicality, streaming restart derivation, clock-only pending reuse/losers, reachable replacement/concurrency rollback, zero-effect transaction, and privacy/capability closure
  pass_3_qa:
    - known/absent/pair-mismatch/unrelated public projection crossed with private mismatch, correct-kind legacy full flow, numeric-only and registry-byte length proofs, reachable replacement, later-history/durable corruption, complete clock/projection/entropy rollback, concurrency/restart/response loss, all-effect-table zero counts, and privacy coverage
  packet_pass_condition:
    - no unresolved Critical, High, Medium, or Low finding
    - all three independent reviewers return PASS; a governance follow-up then explicitly authorizes the exact production/test/fixture scopes
  production_acceptance:
    - TDD, every validation command, sanitized evidence, and all three reviews pass
    - orchestrator may then record delegated acceptance without claiming user personal review

handoff:
  current_state: user approved F002 contract revision; F002 is ready for three independent packet reviews with permissions closed
  after_packet_review: only three zero-finding PASS reviews plus governance authorization may open the exact future scopes
  after_execution: F002 still requires TDD, full validation, evidence, and three implementation reviews; A007/A008 remain non-executable and T202C3/T110 remain unaccepted
  commit_policy:
    - local commits only
    - no push, merge, tag, publish, GitHub Release, live provider, mailbox, credential, or external destructive operation

stop_conditions:
  - Any F002 packet review finds an unresolved Critical, High, Medium, or Low issue, or fewer than three independent PASS reviews exist.
  - Production, test, or fixture work is requested while permissions remain none.
  - A required change touches schema, core transcripts/types, dependencies, runtime, keyring, protocol, CLI, MCP, network, or external calls.
  - Locator material or private origin values would enter output, events, logs, fixtures, evidence, Debug, serialization, cloning, or byte export.
  - Synthetic locator transcript bytes would appear outside authority_registry.rs, or any real/private locator bytes would enter a committed file.
  - A second same-cause contract or validation failure occurs.
